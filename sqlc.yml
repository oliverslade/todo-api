version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "internal/repository/*.sql"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        emit_json_tags: true
        json_tags_case_style: "snake"
